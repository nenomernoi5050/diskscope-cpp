# Security policy

DiskScope is intentionally read-only. It must never delete files, write to a
volume, alter filesystem metadata, or require administrator privileges.

Use it only on data and disk images that you are authorized to inspect. Reports
can contain file paths and matched policy terms; store them according to your
organization's data-handling rules.

Please report a vulnerability privately to `nenomernoi2020@gmail.com`.

